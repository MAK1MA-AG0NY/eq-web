# 耳机模拟工具 — 进度报告

> 状态：**功能已完成并端到端验证通过**
> 涉及项目：`~/eq-web/`（PipeWire 控制面板 + 耳机模拟引擎，已合并为单一项目）

---

## 1. 目标

受 [毁HIFI 混音器](https://huihifi.com/personal/mixer) 启发：**给出两副耳机的频响曲线，算出 EQ，让 A 听起来像 B**。

毁HIFI 只给 20 段且闭源；桌面端可以任意段。目标是：搞清开源现状 → 自己实现一个**经过验证**的版本 → 接进 Ubuntu + PipeWire + `eq-web` 面板 → 把「找同源」和「模拟」做进前端 UI。

**四项全部完成。**

---

## 2. 开源现状结论

**有现成轮子，不需要从零造。** [AutoEq](https://github.com/jaakkopasanen/AutoEq) 支持自定义 target 曲线 → 把 B 的频响当 target 就是模拟 B。参考 [ASR 已验证工作流](https://www.audiosciencereview.com/forum/index.php?threads/headphone-emulation-via-autoeq-squiglink-and-equalizer-apo.62682/)。配套：Squiglink（取数据）、Equalizer APO + Peace（Windows 执行）、REW。

**但 AutoEq 的 Python 包装不上 Python 3.12**：`autoeq 4.1.2` 声明 `requires_python = ">=3.8,<3.12"`，回退到旧版本后 numpy 编译失败。所以自研了一个，并把段数放开到任意。

---

## 3. 已完成

### 3.1 自研 DSP 引擎（现已并入 eq-web）

`fr_core.py`（核心库）、`hp_emulate.py`（CLI）、`verify_fir.py`（FIR 校验）、`README.md`。

### 3.2 实测精度

| 场景 | 20Hz–1k | 1k–10k | 10k–20k |
|---|---|---|---|
| HD650 → HD600 (10 段) | **0.04 dB** | **0.06 dB** | 0.75 dB |
| HD650 → AKG K371 (20 段) | 0.14 dB | 0.25 dB | 3.00 dB |

段数验证：10/15/20/31/40 段均通过。**段数不是越多越准**。
关键自检：**HD650 模拟自己 = 0.00 dB、所有增益 0.00 dB**。

### 3.3 修掉的 bug（都会导致导出错误 EQ）

1. **减法方向写反（最致命）** — EQ 完全反向，但**拟合误差依然完美（0.04 dB）**，因为它忠实拟合了错误的目标。用恒等式 `src + (dst − src) == dst` 做回归测试，现为 2.2e-16。
2. **正则化用错范数** — 惩罚 `gain²` 会**鼓励拆分**（把一个增益拆成多个同频小增益），输出 3 个顶在 20Hz 的滤波器。改用 L1。
3. **`firwin2` 偶数抽头** — Type II 滤波器强制 Nyquist 增益为 0，直接抛异常 → 强制奇数抽头。
4. **绘图基准不一致** — 源/目标未居中、模拟结果已居中，导致模拟结果看起来偏移 2.5 dB。
5. **同频堆叠滤波器** — 重拟合会重新产生 → 增加最后合并；**只合并同号**。
6. **性能** — 首次 10 段拟合超时 600s → 向量化批量求值 + 降采样拟合网格，降到 ~7s。
7. **`sim_get` 属性急切求值** — `sim_get(sim_mod.list_sources)` 在进入函数前就求值，模块缺失时抛 AttributeError（500）而非可读的 503 → 改为传函数名 + `getattr`。
8. **`fr_path()` 404** — 下载路径漏了 category 段（索引里的名字是相对类目目录的）→ 加 `_resolve()` 解析。

### 3.4 PipeWire 集成

- 生成的 conf 与 `eq-web` 的 `_render_conf()` **格式完全一致**，面板自动发现
- CLI 新增 `--pipewire` / `--no-restart` / `--paste-text`
- **解除段数/Q 限制**：实测 PipeWire 可加载 **256 段 / Q=100**，限制纯粹来自面板校验 → 放宽为 `MAX_BANDS=128`、`MAX_Q=50`，魔法数字收敛为命名常量
- 本机已装预设 `66-HIFIMAN-HE400se-→-HIFIMAN-HE1000-Unveiled.conf`（10 段，preamp −13.62 dB），面板 `loaded=True`

### 3.5 面板 UI（本轮完成）

**后端** `~/eq-web/sim.py`（约 420 行）：GitHub 索引（1 次 API 调用，`truncated=False`）+ FR 按需下载缓存 + 调用 DSP 引擎。

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/sim/sources` | 23 个测量源 |
| GET | `/api/sim/headphones?source=` | 某源的耳机列表 |
| GET | `/api/sim/search?q=` | 跨源搜索耳机 |
| POST | `/api/sim/common` | **找同源**：两副耳机的共同源 |
| POST | `/api/sim/run` | 启动模拟（**异步**，返回 job id） |
| GET | `/api/sim/job?id=` | 轮询结果 |

**前端**：`index.html` 新增「🎧 模拟耳机」按钮 + 模拟模态框；`app.js` 新增约 300 行交互逻辑；`style.css` 新增 32 处样式。

UI 设计的关键点：
- **先选测量源，两个耳机列表都限制在该源内** → 从结构上杜绝跨源混用
- 两侧列表各带搜索框（有的源有 700+ 副）
- 「找同源」工具：输入型号 → 列出所有收录它的源 → 一键切换
- 结果区显示低/中/高频 RMS + 预增益 + 耗时
- 走**异步任务 + 轮询**（因为 `server.py` 的 `_handle()` 用 `_REQ_LOCK` 串行化所有请求，同步跑 13 秒会卡死面板）
- 可「创建预设」或「填入编辑器」

---

## 4. 关键技术发现

### 4.1 `HE1000 Unveiled` 存在于 AutoEq

早期结论是错的（当时只查了 oratory1990）。全量索引后确认：
`measurements/Kuulokenurkka/data/over-ear/HIFIMAN HE1000 Unveiled.csv`

### 4.2 同源分析（直接回答你的用例）

```
HE400se 所在源      : Auriculares Argentina, Filk, Kuulokenurkka, Rtings, oratory1990
HE1000 Unveiled 所在源: Kuulokenurkka
             共同源 : Kuulokenurkka   ← 只能用它
```

**这是整件事最重要的前提**：两条曲线必须同源同人工头。混用会把夹具的系统差当成耳机差异。

### 4.3 高频是真实取舍

Kuulokenurkka 源 HE400se → HE1000 Unveiled（10 段）：

| treble_mode | 低频 | 中频 | 高频 | 预增益 |
|---|---|---|---|---|
| `shelf`（默认） | 0.31 | 0.34 | 5.66 | −7.09 dB |
| `ignore` | 0.30 | 0.24 | 6.11 | −8.17 dB |
| `match` | 0.34 | 0.51 | **0.79** | **−13.62 dB** |

`match` 高频准得多，但要多付 6.5 dB 余量，且是在追 10kHz 以上的测量噪声。
**UI 里做成用户可选项，不替他决定。**

### 4.4 正确性证明（不是"看起来对"）

- 拉 PipeWire 源码 `spa/plugins/audioconvert/biquad.c` 逐项比对，数值验证**最大差异 2×10⁻¹¹ dB**
- `bq_lowshelf`/`bq_highshelf` 用**负对照**验证（伪造 label → 0 次 `setup_graph`，真实 → 5 次）
- 预增益用 40 万点密网格在 44.1/48/88.2/96 kHz 独立复核

---

## 5. 部署踩坑（重要）

**systemd 服务用的 `/usr/bin/python3` 没有 numpy/scipy**，而模拟需要它们。

症状：接口报 `{"error": "服务器内部错误: 'NoneType' object has no attribute 'list_sources'"}`。

根因：`ExecStart=/usr/bin/python3 ...` 用的是系统 Python（无 numpy）；而我 shell 里的 `python3` 是另一个项目（Vibe-Trading）的 venv（有 numpy），所以手动测试全过、服务却失败。

**修复**：建独立 venv 并改 unit（不复用别的项目 venv，也不 `--break-system-packages` 污染系统）：

```ini
ExecStart=/home/mak1ma/eq-web/.venv/bin/python3 /home/mak1ma/eq-web/server.py
```

```bash
cd ~/eq-web && /usr/bin/python3 -m venv .venv && .venv/bin/pip install numpy scipy
```

同时把 numpy 改成**惰性依赖**：索引/搜索/同源检查是纯标准库，缺 numpy 也能用，只有 DSP 需要。

---

## 6. 验证结果

### 已验证通过 ✅

| 项 | 结果 |
|---|---|
| CLI 全流程 | 10/15/20/31/40 段通过 |
| 自检（模拟自己） | 0.00 dB，所有增益 0 |
| 双二阶 vs PipeWire | 差异 2×10⁻¹¹ dB |
| shelf 滤波器可用性 | 负对照通过 |
| PipeWire 段数上限 | 256 段 / Q=100 可加载 |
| 6 个 `/api/sim/*` 接口 | HTTP 实测正常 |
| **异步模拟** | 5.5s 完成，返回 10 段 |
| **完整 UI 流程（API 复现）** | 建预设 → 面板 `loaded=True` |
| JS/HTML ID 交叉检查 | 22 个 ID 全部匹配 |
| 静态资源 | `/`、`app.js`、`style.css` 均 200 |
| 原有 6 个预设 | 完好，全部 `loaded=True` |

**实际产出**：`66-HIFIMAN-HE400se-→-HIFIMAN-HE1000-Unveiled.conf`（10 段，preamp −13.62 dB，误差 低0.34/中0.51/高0.79 dB）。

### 未验证 / 已知限制 ⚠️

1. **浏览器点击流未自动化验证** —— 本环境的 Chrome headless 无法正常退出（会挂住整个命令），
   所以没做成真实点击测试。已改用 API 复现 UI 的每一步 + ID 交叉检查 + 静态资源检查替代。
   **建议你手动点一遍确认交互手感。**
2. **未测 20 段以上的 UI 体验** —— 段数越大越慢（20 段约 13s），UI 有轮询但没实测过长时间等待。
3. **`match` 模式的激进滤波器** —— 实测出现 +15.00dB@13.8kHz（顶到增益上限）等大增益，
   预增益 −13.62dB。对音质敏感时建议用 `shelf`。
4. **预设文件名含 `→`** —— 面板用自己的 `_sanitize_name` 生成，只替换 `/` 和空格。
   功能正常但文件名不够干净。
5. **索引依赖 GitHub API** —— 未认证限 60 次/小时。已做 7 天磁盘缓存 + 启动预热，正常使用无影响。

---

## 7. 文件清单

**全部集中在 `~/eq-web/` —— 单一自包含项目，不再依赖任何外部路径。**

| 文件 | 状态 |
|---|---|
| `fr_core.py` | ✅ DSP 引擎（原 hp-emulator，已并入） |
| `hp_emulate.py` | ✅ 同引擎命令行版（已并入） |
| `verify_fir.py` | ✅ 卷积 FIR 校验（已并入） |
| `measurements/` | ✅ 4 个样例频响（CLI 示例用） |
| `SIMULATION.md` | ✅ 引擎与算法完整说明（原 hp-emulator README） |
| `README.md` | ✅ 面板说明 + 模拟功能 + 部署注意 |
| `sim.py` | ✅ 模拟后端（索引/缓存/异步任务/6 接口） |
| `server.py` | ✅ 限制放宽 + `/api/sim/*` 接口 |
| `static/index.html` | ✅ 模拟按钮 + 模态框 |
| `static/app.js` | ✅ 约 300 行交互逻辑 |
| `static/style.css` | ✅ 32 处样式 |
| `start.sh` | ✅ 优先用 `.venv`，缺失时回退并告警 |
| `deploy/eq-web.service` | ✅ 模板同步为 venv 解释器 |
| `~/.config/systemd/user/eq-web.service` | ✅ 已改为 venv 解释器 |
| `.venv/` | ✅ numpy 2.5.3 + scipy 1.18.1 |
| `.gitignore` | ✅ 已加 `.venv/`、`out/` |

### 合并核实结果

- `fr_core.py` / `hp_emulate.py` / `verify_fir.py` 与原文件**逐字节一致**
- `measurements/` 4 个样例一致
- `sim.py` 的 `SIM_DIR` 现在默认解析为 **`~/eq-web`**（不再指向 `~/doc/dsh_workplace/hp-emulator`）
- 全项目已无 `dsh_workplace` / `hp-emulator` 残留引用
- 验证通过：服务端模拟（5.8s）、命令行（RMS 0.04 dB）、FIR 校验、`start.sh`

### 你仓库的注意事项

- `~/eq-web/` 原本就有**未提交改动**（4 个文件、约 507 行）。我只改 `server.py` 的常量区 + **追加** sim 代码，并新增文件，确认与你原有改动不冲突。
- filter-chain 配置备份：`/tmp/filter-chain-backup-1789485215.tgz`
- 之前装的预设 66 曾从磁盘消失；排查结论是**通过面板删除按钮删的**（不在回收站、只有一处删除点、`state.json` 显示它曾设为默认），不是 bug。
- ✅ **`~/doc/dsh_workplace/hp-emulator/` 已删除**（内容全部搬入 eq-web 并逐字节核实后）。
  删除后已复测：服务端模拟 6.4s 正常、命令行 RMS 0.04 dB、7 个预设完好
  —— 证明**无任何残留依赖**，eq-web 现已完全自包含。
